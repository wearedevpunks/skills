# Ownership proof

Materialize the IP-450 project fixture in a run-owned directory and copy the frozen portable verify-behavior entrypoint. Select Repository/Recovery for native interruption, restoration, renewed Doctor, downstream proof, and Cleanup. Keep evidence outside scratch. Then run docs-ingest-phase for ordinary operator docs using the selected Feature Map only as read-only navigation. Compare the complete verifier tree before and after each flow. CLI preservation is exercised by the reserved Harness Vitest public-command test.

For docs navigation, overlay `docs-input/` onto that materialized project and select the ordinary operator-docs maintenance branch. The stale runbook is an intentional input, not accepted recovery guidance. For exact inaccessible-state reporting, materialize `blocked-project/` and select Export without supplying its entitlement; require blocked plus absent downstream output.
