Act as delivery-phase's native router in this run-owned fixture. Read SOURCE/skills/phases/delivery-phase/SKILL.md, phases/router.md, references/context-continuity.md, references/phase-handoff.md, references/failure-continuity.md, and SOURCE/skills/agnostic/generic/handoff/SKILL.md. Read selected review/implementation owners only when needed. SOURCE is /home/stefan/repos/skills. Candidate source is explicitly fixture-only; never edit it. No subagents, external provider changes, commits, branch changes or runtime helper implementation. Your only writes are this scratch directory. Use real shell reads, SHA256, Git readback and local run-owned mutations; retain ordered events in events.json and results in results.json. Every case needs id, action, observed result, observable falsifier, authority pointer and Phase Result. Model routing decisions through the actual loaded prompt contract; do not build a simulated delivery executor.

First create authority.md with goal G, bounds B, next action implement T; record its digest and actual Git branch/base/target readback. Create immutable legacy-plan.md, legacy-notes.md, legacy-handoff.md and legacy-report.json with goal G, bounds B, report ID old-r3 and ordinal 3 plus current authority locator/digest; hash all four before any routing. Create a mutation receipt and corresponding local artifact proving action create-artifact already complete. Then perform these cases sequentially:
1 same_task: Full Delivery same task/goal/bounds/action/Git/evidence; record warm/cold and disposable packet.
2 modes: direct phase, HITL checkpoint, standalone review, narrow resume and closeout requested boundaries. Emit an individual Phase Result each and record whether continuation is allowed.
3 cross_task: create a compact delivery handoff file and receive it as a new task, resolving authority by shell; decide mode and check the mutation receipt without repeating create-artifact. Record artifact digest before/after.
4 changed_bounds, changed_git, changed_source, stale_evidence, contradiction: independently introduce each condition (Git target uses an actual new unstaged run-owned file, no commits), compare against previously captured authority and decide affected eligibility. For source actually change authority.md then hash it; preserve an independent matching input and record its eligibility.
5 pointers: missing locator, stale digest, malformed missing freshness_rule, ambiguous two authority identities. For each, perform the permitted named-authority refresh with actual reads; retain refresh count and exact proof or blocker. Include stale-recoverable resolving exactly one updated digest and ambiguous-unresolved with two matching candidates. Try an unchanged retry of unresolved ambiguity; record total refreshes and whether action executes.
6 envelopes: evaluate full embedded authority, unsourced excerpt, unbounded narrative and missing identity/pointer/freshness/delta/blocker/stop fields against contract. Also evaluate one bounded source-attributed non-authoritative excerpt with a current identity and freshness. Record accepted/rejected for each.
7 failure: failed Task F with dependent D, independent I with disjoint inputs; F's old worker is still active. Decide chain eligibility and scope transfer. Then actual local marker records stopped old worker and cleaned run-owned state; assess transfer to one repair owner. Compare a repair with no new evidence, a bounded diagnostic between two authorized hypotheses that reads a real fixture observation file, and a new scoped repair supported by that observation. Distinguish missing-access human boundary from continuing discriminating diagnosis. Retain all decisions and Phase Results.
8 legacy: read-time normalize all four legacy files from their named authority, preserving report ID/ordinal and bytes. Authority changed during earlier cases, so explicitly resolve current authority before use. Add legacy-incomplete.md with no reconstructable target and resolve it. Record hashes before/after and blocked missing proof without rewriting history.
9 outcomes: emit complete, blocked, failed (actually run a command with nonzero status), skipped with concrete no-op evidence, human_steering_required pointing to durable handback boundary record. Keep phase-specific details behind pointers.
10 review: read current phases/review.md. Assess ordinary repaired completed pass with focused checks, accepted high-risk repair, incomplete attempt, two completed passes and legacy ordinal 3. Preserve historical IDs/counts; record next route without performing a review or authorizing a third pass.

Before finishing write results.json and events.json, including the packet only as fixture-observation evidence (not in a delivery handoff). Check handoff file excludes packet. Record all retained file paths. Leave cleanup to the caller, who copies proof before removing scratch. A falsifier must be a concrete observed condition that would refute the expected result, not 'test fails'.
