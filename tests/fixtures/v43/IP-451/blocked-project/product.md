# Export entitlement

Export requires a run-owned verification-export.entitlement supplied by the fixture operator before Launch. Without it, export is blocked and no output is created. This fixture contains no real credential.
