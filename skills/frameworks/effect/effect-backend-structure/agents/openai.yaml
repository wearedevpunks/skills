interface:
  display_name: "Effect Backend Structure"
  short_description: "Effect backend architecture"
  default_prompt: "Use $backend-domain-structure alongside $effect-backend-structure to shape Effect backend topology and action boundaries; add $effect-service-design when services or Layers are in scope."
