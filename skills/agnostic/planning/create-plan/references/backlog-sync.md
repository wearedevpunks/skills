# Backlog Sync

Use this reference after the plan structure is complete.

## Sync rules

Create or update one backlog item per task.

Track hierarchy and dependencies natively when supported.

Otherwise use structured body text plus cross-links.

Write the planned RED target into each backlog item.

Record created or updated ids and URLs back into the plan.

Prefer the selected backlog system's native CLI or tool when available.
