The public skills repo now groups skills by pack/domain instead of keeping every skill flat under `skills/`. Reason: the CLI already treats packs as the conceptual unit for default/detected skill bundles, so keeping authoring layout aligned with that taxonomy reduces drift and makes overlapping pack semantics explicit. Not every CLI pack maps to a unique folder; some packs reuse the same skill family, such as `drizzle` reusing Effect backend skills.
