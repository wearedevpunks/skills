# Admission fixture product

Product brief: a safe accessible browser slug preview. Target user: a person preparing a URL slug. Objective: show correct normalized output, truthful invalid-input feedback and valid recovery. Product boundaries and accepted behavior are in the retained docs/specs/full-small/SPEC.md. No persistence, authentication or deployment belongs to this product.

Product/Backlog Root: exact isolated private GitHub Projects V2 identity selected by .devpunks/settings.json and verified in CONTEXT.json. Repository: stefan-garofalo/v43-admission-fixture-20260906-01a0777c. Owner: stefan-garofalo. Wiki identity: admission-fixture/browser-slug.

Product Map: Product Area admission-fixture → Initiative browser-slug-preview → each run's browser-preview Epic → one user-outcome Story → one atomic vertical Task. The provider IDs vary per run and are retained in the current Backlog Result; this map fixes meaning, not provider identity. Source outcome: OUT-001.

Operating rules: each native comparison receives separate provider objects and branch; preserve all earlier run histories. Each Story/Task pair shares its run's V* milestone and exact empty blocker set. Milestone goal is complete safe browser slug preview. Current iteration identity and provider state come from exact current CONTEXT/readback; future iterations are outside scope. No Finder/Fog intake was supplied. No merge, release or reusable-source publication is authorized.
