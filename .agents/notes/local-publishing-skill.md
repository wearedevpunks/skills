# Local Publishing Skill

`publish-skills-baseline` is intentionally local operator guidance, not public skill content. Keep the source at `.agents/skills/publish-skills-baseline/` and expose it to Codex/Claude through `.codex/skills/` and `.claude/skills/` symlinks. Do not copy it into the repo `skills/` tree because that tree is synced into `wearedevpunks-cli` and released in scaffold baselines.
