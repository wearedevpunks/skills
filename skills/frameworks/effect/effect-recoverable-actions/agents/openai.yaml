interface:
  display_name: "Effect Recoverable Actions"
  short_description: "Recoverable multi-step Effect action flows"
  default_prompt: "Use $effect-recoverable-actions for `packages/api` action flows that chain multiple dependent operations and explicitly need a transaction, retry, rollback, compensation, or failure-path testing strategy."
