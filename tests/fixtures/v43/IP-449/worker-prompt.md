Act as the implementation worker in a native prompt-contract fixture. Read the source root recorded in source-root.txt plus skills/agnostic/planning/implement-spec/references/parallel-worker-brief.md as the public workflow authority. This is task-context validation, not production implementation. You own only worker-results.json in the current scratch directory. Each cases.json entry is one brief with intended outcome READY, no code changes, no dependencies, local context validation and tdd_status not_applicable; all other files are read dependencies or parent-owned canonical records. Inspect the selected authority and assess every case before consumption. Refresh recoverable context from source, retain original invalidity and refreshed identity explicitly; return blocked when authority cannot resolve. Return per-case Task Results including observed result, source identity, decision, invalidation/revalidation needed and exact evidence. Record exact paths/sections read. Do not perform parent reconciliation. No subagents. Finish after worker-results.json is written.